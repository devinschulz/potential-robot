%h2= link_to article.title, article
%time{datetime: article.date.strftime('%Y-%m-%d')}= article.date.strftime('%b %e')

- if article.tags.present?
  - article.tags.each do |tag|
    = link_to(tag, tag_path(tag))

= article.body